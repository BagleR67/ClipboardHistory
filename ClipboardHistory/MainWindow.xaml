<Window x:Class="ClipboardHistory.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClipboardHistory"
        mc:Ignorable="d"
        Title="CH Windows" Height="450" Width="800">
    <Grid>
        <Grid Name="mainGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Margin="4,8" Text="Поиск:"/>
                <TextBox Grid.Column="1" Margin="4,8" Background="LightGray"/>
                <Button Grid.Column="2" Content="Найти" Margin="4,8" Width="100"/>
            </Grid>

            <ListBox x:Name="mainListBox"  Grid.Row="1" ItemsSource="{Binding}" DataContext="clips" MouseDoubleClick="mainListBox_MouseDoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="4" Orientation="Vertical">
                            <TextBlock Text="{Binding CopiedAt, StringFormat='{}{0:HH:mm}'}" Margin="5"/>
                            <TextBlock Text="{Binding Text}" Margin="5"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                
            </ListBox>

            <StackPanel Grid.Row="2" Orientation="Horizontal">
                <TextBlock Text="Всего элементов: " Margin="4,8"/>
                <TextBlock Name="countElements" Text="0" Margin="4,8"/>
            </StackPanel>
            

        </Grid>


    </Grid>
</Window>
